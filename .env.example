# ============================================
# Configuración de Entorno - Orden de Autorización y Compra
# ============================================

# Node Environment
NODE_ENV=production

# Application
PORT=3000
HOSTNAME=0.0.0.0

# Next.js
NEXT_TELEMETRY_DISABLED=1

# Authentication (NextAuth)
# IMPORTANTE: Genera un secret seguro con: openssl rand -base64 32
AUTH_SECRET=RUN-SOLUTIONS-NEARLINK360-SECRET-KEY-CHANGE-IN-PRODUCTION

# URLs (Actualizar con tu dominio)
# NEXT_PUBLIC_APP_URL=https://your-domain.com
# NEXT_PUBLIC_API_URL=https://your-domain.com/api

# PM2 Configuration
PM2_INSTANCES=2

# Logging (Opcional)
# LOG_LEVEL=info

# Database (Si agregas base de datos en el futuro)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname

# Email (Si agregas notificaciones por email)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# Storage (Si necesitas almacenamiento de PDFs)
# STORAGE_TYPE=local
# STORAGE_PATH=/var/data/pdfs

# ============================================
# Notas:
# - Copia este archivo a .env.local para desarrollo
# - Copia este archivo a .env.production para producción
# - NUNCA subas archivos .env al repositorio
# ============================================
